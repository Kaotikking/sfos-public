# SFOS Public Debian Base

This repository currently contains only the target-neutral Debian Base substrate for SFOS.

It contains no Outpost, Kernel, Serein domain, model, credential, machine identity, gateway, or Stage-1 payload. Those are rebuilt separately only after complete local proof and canonical admission.

The Debian Base remains pinned to signed Debian release data and preserves interactive destructive-storage confirmation.
